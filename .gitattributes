# Normalize line endings: LF in the repository and in the working tree.
# Overrides any local core.autocrlf setting so every checkout matches Biome's
# `formatter.lineEnding: "lf"` and `biome check` stays clean on Windows.
* text=auto eol=lf

# Binary assets — never touch their bytes.
*.png binary
*.webp binary
*.ico binary
